import { Metadata } from "next";

import { default as SocialPage } from "@/components/SocialProgram/SocialProgram";

const metadata: Metadata = {
  title: "Social",
};

export { SocialPage as default, metadata };
